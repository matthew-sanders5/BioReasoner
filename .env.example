# Copy this file to .env (DO NOT commit .env)
# This project is safe to run without any paid services.

# --------------------------------------------------
# Provider selection
# --------------------------------------------------
# Options:
#   - "stub"   : fully offline, deterministic (recommended for reproducibility)
#   - "ollama" : local Ollama server
#   - "openai" : OpenAI API (optional, paid)
BIOREASONER_MODEL_PROVIDER="stub"

# Model name (meaning depends on provider)
#   - stub   : use "stub"
#   - ollama : e.g. "llama3.1", "mistral"
#   - openai : e.g. "gpt-4.1-mini"
BIOREASONER_MODEL_NAME="stub"

# --------------------------------------------------
# OpenAI (optional)
# --------------------------------------------------
# Required ONLY if BIOREASONER_MODEL_PROVIDER="openai"
OPENAI_API_KEY=""

# --------------------------------------------------
# Ollama (optional)
# --------------------------------------------------
# Required ONLY if BIOREASONER_MODEL_PROVIDER="ollama"
BIOREASONER_OLLAMA_BASE_URL="http://localhost:11434"
